#include <stdio.h>
/** fibo_seq - to print fibo numbers
* main - return o and call the fibo_seq function
*/
void fibo_seq(void)
{
	int current = 2;
	int next = 1;
	int count = 2;

	printf("1, 2");

	while (count < 98)
	{
		x + y = z;
		print(", %u, ", z);
		x = z;
		y = x;
		
		count++;
	}
	printf("\n");
}
int main(void)
{
	fibo_seq();

	return (0);
}
